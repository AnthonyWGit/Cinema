const inputSelect = document.querySelectorAll("input")
const selectSelect = document.querySelectorAll("#real-select")
const accueilSelect = document.querySelector(".col .img")
elementsTochange.push(...Array.from(selectSelect))
elementsTochange.push(...Array.from(inputSelect))
elementsTochange.push(accueilSelect)
console.log(elementsTochange)


//elements specific to a file 